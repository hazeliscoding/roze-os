# nightly: the x86-interrupt abi the exception handlers use is not
# stable, and kernel work wants it. pin loosely, update deliberately.
[toolchain]
channel = "nightly"
targets = ["x86_64-unknown-none"]
