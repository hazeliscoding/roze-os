# limine bootloader config. zero timeout, straight into the kernel.

# serial stays on so bootloader panics land in the log with the
# kernel output instead of dying silently on screen
timeout: 0
serial: yes

/RozeOS
    protocol: limine
    path: boot():/boot/kernel
    module_path: boot():/boot/doom1.wad
