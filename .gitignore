# Build output
/target/

# Limine binaries (fetched by xtask)
/limine/

# Game data, never committed
assets/*.wad

# Spec / planning documents (local only, never committed)
/specs/

# AI tooling (local only, never committed)
CLAUDE.md
CLAUDE.local.md
.claude/
.claude-plugin/
AGENTS.md
.cursor/
.cursorrules
.aider*
.github/copilot-instructions.md
